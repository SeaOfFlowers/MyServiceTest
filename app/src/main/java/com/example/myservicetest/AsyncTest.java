package com.example.myservicetest;

import android.graphics.Bitmap;
import android.os.AsyncTask;

public class AsyncTest extends AsyncTask<String,Integer, Bitmap> {

    @Override
    protected Bitmap doInBackground(String... strings) {
        return null;
    }
}
